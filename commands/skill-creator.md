---
description: Create or update an OpenCode skill using the bundled opencode-skill-creator workflow
agent: gentle-orchestrator
subtask: true
---

Load `opencode-skill-creator` first, then use it to create or update an OpenCode skill from the user's request.

If the request is ambiguous, ask one focused clarification before editing files.
