{
  "name": "gentleman-vMK-agent-gh",
  "version": "1.0.0",
  "description": "Curated opencode agent config: 68 skills + _shared shared references, prompts, scripts, and protocols. Synchronized with global opencode at C:\\Users\\MK\\.config\\opencode\\",
  "private": true,
  "author": "Luis Alberto",
  "license": "UNLICENSED",
  "scripts": {
    "test:bash-safe": "pwsh -NoProfile -Command \"& { . .\\scripts\\bash-safe.ps1; $r=0; foreach (\\$t in @('echo a','echo a && echo b','echo a || echo b','echo hi | Select-String h')) { try { Invoke-Bash \\$t -ErrorAction Stop; 'PASS' } catch { 'FAIL'; \\$r=1 } }; exit \\$r }\"",
    "tokenize": "pwsh -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\tokenize-all.ps1",
    "expand-config": "pwsh -NoProfile -ExecutionPolicy Bypass -File .\\scripts\\opencode-config\\expand-config.ps1",
    "postinstall": "npm run expand-config"
  },
  "engines": {
    "opencode": ">=1.18.24"
  },
  "files": [
    "AGENTS.md",
    "ANTI-PATTERN-CATALOG.md",
    "BITACORA.md",
    "docs/CHANGELOG.md",
    "SKILLS-INDEX.md",
    "prompts/",
    "scripts/",
    ".agents/skills/"
  ],
  "keywords": [
    "opencode",
    "agent-config",
    "karpathy-compression",
    "sdd"
  ],
  "devDependencies": {
    "@modelcontextprotocol/server-sequential-thinking": "2026.7.4",
    "@playwright/test": "^1.62.1",
    "playwright": "1.61.1"
  }
}
