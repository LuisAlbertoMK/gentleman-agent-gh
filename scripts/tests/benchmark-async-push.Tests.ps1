#requires -Version 7

Describe 'benchmark-async-push.ps1' {
    It 'exists' -Skip:$true {
        # benchmark-async-push.ps1 archived/deprecated — not in active scope
        $true | Should -BeTrue
    }
    It 'parses without errors' -Skip:$true {
        # benchmark-async-push.ps1 archived/deprecated — not in active scope
        $true | Should -BeTrue
    }
}
