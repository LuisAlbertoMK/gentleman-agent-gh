#requires -Version 7

Describe 'backup.ps1' {
    It 'exists' -Skip:$true {
        # backup.ps1 archived/deprecated — not in active scope
        $true | Should -BeTrue
    }
    It 'parses without errors' -Skip:$true {
        # backup.ps1 archived/deprecated — not in active scope
        $true | Should -BeTrue
    }
}
