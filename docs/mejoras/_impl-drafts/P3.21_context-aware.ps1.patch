# DRAFT — awaiting CI/PSSA/Pester validation (node/pwsh=negated local)
# P3.21 — Context-aware skill table lazy (patch draft, NOT applied)

**Scope lock**: `.agents/skills/` + `scripts/build-skill-registry.ps1` — reemplazar tabla completa de 166 skills en system prompt por índice de triggers compacto (sparse-load real, P2.14 del plan).
**Cita plan**: plan-kimi-k3.md:94 (P2.14) — "Skill table lazy: reemplazar tabla completa de 166 skills en system prompt por índice de triggers compacto (sparse-load real)"; :63 (F15 166 skills inline en cada prompt).
**Blast**: Medio (cambia sistema de carga de skills — afecta descubrimiento). **Regla**: commit `feat(skills):`.

```diff
--- a/scripts/build-skill-registry.ps1
+++ b/scripts/build-skill-registry.ps1
@@ (generación de tabla, aprox — verify at apply) @@
-  $table = $skills | ForEach-Object { "| $($_.Name) | $($_.Desc) |" }   # 166 rows
+  $table = ($skills | Group-Object Trigger | ForEach-Object { "| $($_.Name) | $($_.Group[0].Desc) |" })  # índice compacto
```

**Idempotencia**: regeneración determinista desde frontmatter; mismo inventario → mismo índice.
**Rollback**: git revert + regenerar. **DoD**: system prompt sin tabla 166 (token save), skill-resolver-fast sigue resolviendo todos.
**CI fallback (local negado)**: `gh workflow run quality-gate.yml` ; `pwsh -c "./scripts/build-skill-registry.ps1 -Verify"` + test skill-resolver.
